# Changelog

All notable changes to this project should be documented in this file.

The default Openboost convention is Semantic Versioning (`MAJOR.MINOR.PATCH`) unless the target repository already uses another documented versioning policy.

## [Unreleased]

### Added

### Changed

### Fixed

### Removed

### Security

## [0.1.0] - YYYY-MM-DD

### Added
- Initial release.

---

Release rules:

- Add meaningful user/developer-visible changes under `[Unreleased]` while developing.
- At release time, move the relevant entries into `## [X.Y.Z] - YYYY-MM-DD`.
- Do not list every changed file or internal line edit.
- Keep published version sections immutable except for correcting documentation mistakes that do not falsify release history.
- A published tag/version should not be silently reused for different code.
